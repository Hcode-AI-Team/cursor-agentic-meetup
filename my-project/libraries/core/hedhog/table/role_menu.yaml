columns:
  - type: pk
  - name: role_id
    type: fk
    references:
      table: role
      column: id
      onDelete: CASCADE
  - name: menu_id
    type: fk
    references:
      table: menu
      column: id
      onDelete: CASCADE
  - type: created_at
  - type: updated_at
